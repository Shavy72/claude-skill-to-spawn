---
name: meta-exec
description: "Alter Name für /to-spawn — Bau-Sessions einer Spec starten. Trigger: /meta-exec, „meta-exec“."
disable-model-invocation: false
---

# /meta-exec — Alias auf `/to-spawn`

Alter Name, zeigt auf `/to-spawn` — dort weiterlesen (Skill `to-spawn`). Keine eigene Logik.

Kurz: `/to-spawn <S>` fragt „lokal (1) oder Server (2)?“ · `/to-spawn-local <S>` · `/to-spawn-remote <S>`.
